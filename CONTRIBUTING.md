# Contributing

Create a short-lived `feature/`, `fix/`, `docs/`, or `chore/` branch and open a
pull request into `main`. Use Conventional Commits, include tests and public API
impact, and do not merge until required checks pass and conversations resolve.

